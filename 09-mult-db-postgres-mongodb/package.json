{
  "name": "09-mult-db-postgres-mongodb",
  "version": "1.0.0",
  "description": "docker run \\     --name postgres \\                 ## nome do serviço docker imagem     -e POSTGRES_USER=junior \\         ## cria variavel de ambiente para configura use do banco     -e POSTGRES_PASSWORD=Bwi280281 \\  ## cria variavel de ambiente para configura senha do banco     -e POSTGRES_DB=heroes \\           ## cria variavel de ambiente para configura nome  do banco     -p 5432:5432 \\                    ## configura a porta inter:externa(expoe)     -d \\                              ## coloca para roda em segundo plano       postgres                          ## Passa o nome da imagem para baixar",
  "main": "index.js",
  "scripts": {
    "test": "mocha src/tests/*.js",
    "test:watch": "mocha src/tests/*.js -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^5.8.7",
    "pg": "^7.17.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^5.21.3"
  },
  "devDependencies": {
    "mocha": "^10.2.0"
  }
}
